fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
httpx==0.27.0
python-dotenv==1.0.1
python-multipart==0.0.20
sqlmodel==0.0.22
passlib==1.7.4
python-jose[cryptography]==3.3.0
# Pure-Python PDF text extraction for .pdf uploads (no system deps; Vercel-safe).
pypdf==5.1.0
# Postgres driver (used only when DATABASE_URL=postgresql+psycopg://...).
# Recommended on Vercel for durable data, since the default SQLite lives in ephemeral /tmp.
psycopg[binary]==3.2.3
